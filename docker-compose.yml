services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: softcomerce
      POSTGRES_PASSWORD: softcomerce
      POSTGRES_DB: softcomerce
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
