# PRODUCTION Environment Variables for Vercel
# Set these in Vercel Dashboard → Settings → Environment Variables

# After deploying backend to Render, replace the URL below with your actual Render URL
NEXT_PUBLIC_API_URL=https://api.softkart.nitechspark.site
